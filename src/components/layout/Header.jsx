import NavMenu from './NavMenu';

const Header = () => {
    return (
        <header className="header">
            <h1>Get Fluent</h1>
            <NavMenu />
        </header>
    );
};

export default Header;